import RegisterForm from "./registerForm";

export default () => {
    return (
        <section id={"register-page"} className={"h-screen w-full bg-stone-200 flex justify-center items-center"}>
            <RegisterForm />
        </section>
    )
}