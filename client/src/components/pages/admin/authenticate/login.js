import LoginForm from "./loginForm";

export default () => {
    return (
        <section id={"login-page"} className={"h-screen w-full bg-stone-200 flex justify-center items-center"}>
            <LoginForm />
        </section>
    )
}